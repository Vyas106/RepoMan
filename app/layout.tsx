import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { AuthProvider } from "@/contexts/auth-context"
import { Toaster } from "@/components/ui/toaster"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
})

export const metadata: Metadata = {
  title: "DevCollab - Developer Collaboration Platform",
  description:
    "A comprehensive platform for developers, project managers, and product managers to collaborate effectively with AI-powered insights.",
  keywords: ["collaboration", "development", "project management", "github", "ai"],
  authors: [{ name: "DevCollab Team" }],
  openGraph: {
    title: "DevCollab - Developer Collaboration Platform",
    description: "Collaborate smarter, build better with AI-powered insights",
    type: "website",
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={inter.variable} suppressHydrationWarning>
      <body className={`${inter.className} antialiased`}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          <AuthProvider>
            {children}
            <Toaster />
          </AuthProvider>
        </ThemeProvider>
      </body>
    </html>
  )
}
